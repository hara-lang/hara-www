SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
SITE_ROOT=$(CDPATH= cd -- "$SCRIPT_DIR/../.." && pwd)
WORKSPACE_ROOT=${HARA_WORKSPACE_ROOT:-$(CDPATH= cd -- "$SITE_ROOT/../.." && pwd)}
HARA_ROOT="$WORKSPACE_ROOT/technology/hara"
ROOT="$HARA_ROOT"
export SITE_ROOT WORKSPACE_ROOT HARA_ROOT ROOT
