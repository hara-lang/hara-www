(ns+
  (:require [studio.draw :as draw]))

(node/start
  (fn []
    (let [frame (co/await (draw/next-frame "canvas/background"))]
      (co/await
        (draw/render
          "canvas/background"
          {:type :canvas-2d
           :background "#020408"
           :commands []})))))
